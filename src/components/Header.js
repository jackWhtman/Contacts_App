import React from "react";

const Header = () => {
  const activeStyle = { color: "#F15B2A" };
  return (
    <header className ="jumbotron jumbotron-responsive">
      <h1>ContactsApp</h1>
    </header>
  );
};

export default Header;
